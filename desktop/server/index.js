#!/usr/bin/env node
// Stub entry_point required by the MCPB manifest schema. The actual MCP
// transport runs via `npx mcp-remote` declared in manifest.json `mcp_config`,
// which proxies stdio to the hosted Covy server. This file is never executed.
